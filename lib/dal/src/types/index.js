import { categories, colors, discounts, makers, makeCountries, productColors, products, measurementUnits, relatedProducts, catalogVersion, } from "../schema/catalog";
import { cartCheckout, cartCheckoutItems, guests, notificationAddressees, notifications, readReplicas, operationsVersion, } from "../schema/operations";
import { info, generalVersion } from "../schema/general";
