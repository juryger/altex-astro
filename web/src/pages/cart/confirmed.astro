---
export const prerender = false;

import BaseLayout from '../../layouts/BaseLayout.astro';
import { Image } from 'astro:assets';
import successLogo from '../../assets/success.png';
import { extractUrlParam } from '../../core/utils/url-parser';

const order = extractUrlParam(new URL(Astro.request.url), "order", "string"); 
console.log("🛠️ ~ Cart checkout confirmation page ~ request: %s", order);
---

<BaseLayout title="Подтверждение заказа">
  <div class="flex gap-2">
    <div class="grow-0 content-start">
      <Image src={successLogo}
          class="w-32"
          alt="Confirmation logo"
        />
    </div>
    <div class="grow-1 content-center">
      <p>
        <span>Спасибо за раземещение заказа с номером <b>{order}</b>. В ближайшее время мы свяжимся с Вами по указанном email адресу для уточнения деталей.</span>
      </p>
    </div>
  </div>
</BaseLayout>
