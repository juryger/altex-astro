---
export const prerender = false;

import Categories from '@/web/src/components/catalog/Categories.astro';
import CollectionFallback from '@/web/src/components/CollectionFallback.astro';
import BaseLayout from '@/web/src/layouts/BaseLayout.astro';
import { defaultSorting } from '@/web/src/core/models/sorting';
import { defaultPaging } from '@/web/src/core/models/paging';

const pageTitle = "Каталог";
const subTitle = "Список категорий";
---

<BaseLayout title={pageTitle}>
  <Categories server:defer>
		<div slot="fallback" class="mx-3.5">
			<CollectionFallback title={subTitle} />
		</div>
	</Categories>
</BaseLayout>
