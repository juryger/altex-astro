---
import { Image } from 'astro:assets';
import AltexLogo from '../assets/altex-logo-md.png';
import { getNavPathResolver } from '../core/helpers/nav-path-resolver';

const allPaths = getNavPathResolver().getAllPaths({ isMainMenu: false, isFooter: true, isBreadcrumbs: false });
---

<footer class="footer footer-horizontal footer-center bg-base-300 text-base-content rounded pt-2 pb-2">
  <nav class="grid grid-flow-col gap-4">
    {allPaths && allPaths.map((x) => (
      <a class="link link-info link-hover" href={x.path}>{x.value}</a>
    ))}
  </nav>
  <aside class="grid-flow-col items-center">
    <Image src={AltexLogo} class="w-16" alt="Footer logo"/>
    <p>
      ИП Герасимов Алексей Владимирович 
      <br />
      Поставка надежных товаров и услуг с 1995 г.
    </p>
  </aside>
  <aside>
    <p>Copyright © {new Date().getFullYear()} - Все права защищены</p>
  </aside>
</footer>